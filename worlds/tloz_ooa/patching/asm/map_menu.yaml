# map pop-up icons for seed trees
# present
02/6d05/: db locations.crescentTree.id - $0b
02/6d08/: db locations.symmetryTree.id - $0b
02/6d0b/: db locations.lynnaTree.id - $0b
02/6d0e/: db locations.zoraTree.id - $0b

# past
02/6d20/: db locations.ridgeWestTree.id - $0b
02/6d23/: db locations.palaceTree.id - $0b
02/6d26/: db locations.ridgeEastTree.id - $0b
02/6d29/: db locations.lynnaTree.id - $0b
02/6d2c/: db locations.forestTree.id - $0b
02/6d2f/: db locations.zoraTree.id - $0b


# Store a few additional variables when checking the SELECT button to open map
02//extendedMenuOpenCheck: |
  ld a,(wDungeonIndex)
  ld (wOriginalDungeonIndex),a
  ld a,(wMinimapGroup)
  ld (wOriginalMinimapGroup),a
  ld a,(wKeysJustPressed)
  ret
# Replace the second `ld a,(wKeysJustPressed)` in b2_updateMenus by an extension
02/4fe7/: call extendedMenuOpenCheck

38//minimapTableCycle: db $0D,$01,$02,$03,$04,$05,$06,$0C,$07,$08,$0B,$FF

# When booting the map, handle a "cycling order" that can be issued using the keybinding defined
# by above functions. Pressing that button reopens the map with another map mode,
# and the cycling between modes is addressed by this function.
38//extendedBootMapMenu_body: |
  
  ; Change the current map mode in case we are reopening the map after a "cycling order"
  ld a,(wMinimapCycleToNextMode)
  or a
  jr z,@open
  
  ld a,(wMinimapGroup)
  cp $04
  jr c,@notDungeon
  
  @dungeon:
  ld a,(wDungeonIndex)
  ld hl,minimapTableCycle
  ld e,$00
  call searchKey
  jr nc,@invalidDungeon ; currently in a dungeon that is not in the cycle, cycling to overworld
  ld a,(hl)
  
  /ifdef d11
  cp $ff
  jr z,@invalidDungeon ; Cycled to the end of the list (d11 included)
  /else
  cp $0b
  jr z,@invalidDungeon ; Cycled to the end of the list (d11 excluded)
  /endif

  ld (wDungeonIndex),a
  jr @openDungeon
  
  @invalidDungeon:
  xor a
  ld (wMinimapGroup),a
  jr @open
  
  @notDungeon:
  ld a,(wMinimapGroup)
  inc a
  cp $02
  jr nc,@swapToDungeon
  
  ld (wMinimapGroup),a
  jr @open
  
  @swapToDungeon:
  ld a,$05
  ld (wMinimapGroup),a
  ld a,$0D
  ld (wDungeonIndex),a
  
  @openDungeon:
  xor a
  ld (wDungeonMapScroll),a
  ld (wDungeonMapScrollState),a
  
  @open:
  call disableLcd
  ret
  
02//extendedBootMapMenu_head: |
  ; Wait for palette change to complete
  ld a,(wPaletteThread_mode)
  or a
  ret nz

  push hl
  push de
  ld e,$38
  ld hl,extendedBootMapMenu_body
  call interBankCall
  pop de
  pop hl
  jp $6014    ; mapMenu_state0

# Change the pointer to state0 inside runMapMenu jump table
02/6010/: dw extendedBootMapMenu_head

# Handle a keybinding to change map type (overworld, Subrosia, dungeons)
02//worldMapStartButtonSupport: |
  jp nz,$4fba  ; _closeMenu
  ld a,(wKeysJustPressed)
  cp BTN_START
  ret nz
  
  ; Put back the menu in "loading" state while cycling to next mode
  ld a,$01
  ld (wMinimapCycleToNextMode),a
  xor a
  ld (wMenuActiveState),a
  call fastFadeoutToWhite
  ret
# Replace `jp nz,closeMenu` in @noDirectionButtonPressed by an extension (outside maps)
02/618b/: call worldMapStartButtonSupport
# Replace `jp nz,closeMenu` in @dungeon by an extension (dungeon maps)
02/6133/: call worldMapStartButtonSupport

# Extend the close menu procedure to reset temporarily changed variable with the value
# they had when initially opening the map
02//extendedMenuClose: |
  ; Reset minimap group & dungeon index to their original value
  ld a,(wOriginalDungeonIndex)
  ld (wDungeonIndex),a
  ld a,(wOriginalMinimapGroup)
  ld (wMinimapGroup),a
  xor a
  ld (wMinimapCycleToNextMode),a
  
  ld hl,wMenuLoadState
  ret
# Replace `ld hl,wMenuLoadState` in menuStateFadeOutOfMenu by an extension
02/50ca/: call extendedMenuClose

# Tests if player really is in the dungeon that is being looked at through the dungeon
# map (sets flag Z if inside)
02//checkIfInsideDungeon: |
  ld a,(wDungeonIndex)
  cp $ff
  jr z,@invalid
  
  @valid:
  ld b,a
  ld a,(wOriginalDungeonIndex)
  cp b
  ret
  
  @invalid:
  or a
  ret

# Only draw dungeon map if it's the dungeon we are currently in (because technical reasons)
02//dontDrawDungeonIfNotInside: |
  call checkIfInsideDungeon
  jr nz,@done
  call $67de      ; dungeonMap_generateScrollableTilemap
  call $677c      ; dungeonMap_drawFloorList
  call $682c      ; dungeonMap_updateScroll
  @done:
  ret
# replace the unique calls of the 3 functions contained inside dontDrawDungeonIfNotInside by a
# call to it
02/608c/: |
  call dontDrawDungeonIfNotInside
  jr $04

# Only draw annex sprites in dungeon map (Link icon, cursors and boss symbols...) if we currently
# are in the dungeon
02//dontDrawDungeonSpritesIfNotInside: |
  call checkIfInsideDungeon
  jr nz,@done
  call $657f     ; dungeonMap_drawLinkIcons
  call $65d5     ; dungeonMap_drawCursor
  call $65fd     ; dungeonMap_drawArrows
  call $6566     ; dungeonMap_drawBossSymbolForFloor
  call $654a     ; dungeonMap_drawFloorCursor
  @done:
  ret
# Replace the only call to dungeonMap_drawLinkIcons by the extension above
02/64b8/: jp dontDrawDungeonSpritesIfNotInside

# Prevent from scrolling floors if not inside dungeon (and therefore map is not displayed)
02//onlyAllowScrollingIfInsideDungeon: |
  call checkIfInsideDungeon
  jr nz,@done
  jp $0294    ; getInputWithAutofire
  
  @done:
  pop af      ; pop return address from stack
  ret
# Replace first call of dungeonMap_scrollingState0 by this extension
02/63da/: call onlyAllowScrollingIfInsideDungeon

# On Present & Past maps, don't draw the arrow if we aren't in that dimension
02//drawWorldArrowOnlyIfInDimension: |
  ld a,(wMinimapCycleToNextMode)
  or a
  jr nz,@differentDimension
  ld a,(wFrameCounter)
  ret
  @differentDimension:
  pop af      ; pop return address from stack
  ret
# Replace the first instruction of mapMenu_drawArrow by a call to above extension
02/664e/: call drawWorldArrowOnlyIfInDimension

# Override initialization of cursor position to set it to origin position if we have cycled
# mode at least once
02//initializeCursorPosition: |
  ld (wMapMenuCursorIndex),a
  ld a,(wMinimapCycleToNextMode)
  or a
  jr z,@done
  xor a
  ld (wMapMenuCursorIndex),a
  @done:
  ret
# Replace the instruction `ld (wMapMenu.cursorIndex),a` inside mapMenu_state0 by a call to above function
02/6055/: call initializeCursorPosition

# If dungeon map is owned, return with flag Z unset to indicate dungeon has
# been visited and needs to be displayed on map. Otherwise, perform the usual
# @checkDungeonEntered which tests if dungeon has REALLY been visited
02//extendedCheckForDungeonDisplay: |
  ld hl,wDungeonMaps
  call checkFlag
  ld a,c
  ret nz
  jp $6221    ; @checkDungeonEntered
# Replace the unique call to @checkDungeonEntered by a call to above extension
02/61fb/: |
  /ifdef showDungeonWithMap
  call extendedCheckForDungeonDisplay
  /endif

# replace jr nz,+ in specialCode1 by a simple jr
02/61fe/: db $18

02//alwaysShowDungeonTileContent: |
  ld a,($cbb6) ; (wMapMenu.cursorIndex)
  ld c,a
  ld hl,$6c37 ; presentMinimapPopups
  ld a,(wMapMenu_mode) 
  rrca
  jr nc,@overworld
  ld hl,$6c94 ; pastMinimapPopups
  @overworld:
  @loop:
  ldi a,(hl)
  cp $ff
  jr z,@noIcon
  cp c
  ldi a,(hl)
  jr nz,@loop
  
  and $0f
  cp $08
  jr nz,@noIcon
  or a
  ret
  
  @noIcon:
  jp $6639 ; mapMenu_checkRoomVisited

02/6636/: jp alwaysShowDungeonTileContent


02//setMapMode: |
  ld a,(wMinimapGroup)
  cp $02
  jr c,@setmode
  ld a,$02
  @setmode:
  ld (wMapMenu_mode),a
  ret

02/60d8/: call setMapMode

# display portal popup map icons for bridge builders' screen present and
# symmetry city past.
#02//displayPortalPopups: |
#    ld a,(wMapMenu_mode)
#    and a
#    ld a,(wMapMenu_cursorIndex)
#    jr nz,@present
#    cp $25
#    jr nz,@ret
#    ld a,$aa
#    jr @gotIcon
#    @present:
#    cp $13
#    jr nz,@ret
#    ld a,$a3
#    @gotIcon:
#    jp $6255
#    @ret:
#    jp $6248
## tree warp / map stuff
#02/6245/: jp displayPortalPopups

# Disabled because we are missing some space on bank 02. Will activate it later